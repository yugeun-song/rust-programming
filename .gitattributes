# Normalize line endings to LF across platforms on checkout and commit.
* text=auto eol=lf

# Cargo.lock is generated: keep it out of language statistics and collapse its diffs.
Cargo.lock linguist-generated=true
